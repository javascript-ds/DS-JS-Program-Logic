function addTo(n) {
    let total = 0;
    for(let i=1;i<=n;i++) {
        total = total+i;
    }
    return total;
}
console.log(performance.now());
console.log(addTo(15));
console.log(performance.now());
